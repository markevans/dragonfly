source 'https://rubygems.org'

gemspec

group :development do
  gem 'pry'
  gem 'rake'
  # No longer in the standard library as of ruby 4.0
  # (needed by rack/mock and activerecord respectively)
  gem 'cgi'
  gem 'benchmark'
  # Needed by irb, and no longer a default gem as of ruby 4.0
  gem 'rdoc'
end
